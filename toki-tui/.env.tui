# TUI Development Environment
# This file configures the TUI to use an ISOLATED database
# Your production database (toki) will NOT be touched

# ⚠️ SAFETY: This points to a separate dev database
DATABASE_URL=postgres://postgres:password@localhost:5432/toki_tui_dev

# Milltime Configuration (copy from toki-api/.env.example if needed)
MILLTIME_URL=https://your.milltime.instance/cgi/mt.cgi
MT_CRYPTO_KEY=your_base64_encoded_crypto_key

# Development: Disable auth for quick testing
# Set to 'false' when you want to test authentication
DISABLE_AUTH=true
